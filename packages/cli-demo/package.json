{
  "name": "@report-suite/cli-demo",
  "version": "1.0.0",
  "description": "CLI demo for report generation",
  "bin": {
    "report-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@report-suite/engine": "workspace:*",
    "puppeteer": "^21.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0"
  }
}